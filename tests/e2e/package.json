{
  "name": "megaman-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end browser tests for the megaman WASM build. Validates the full OpenCoreGraphics → OpenCoreAnimation → OpenSpriteKit → megaman pipeline.",
  "type": "module",
  "scripts": {
    "serve": "node server.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
